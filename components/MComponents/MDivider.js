import * as React from 'react';
import { View } from 'react-native';
import { Divider, Text } from 'react-native-paper';

const MDivider = (props) => (
    <Divider {...props} />
);

export default MDivider;